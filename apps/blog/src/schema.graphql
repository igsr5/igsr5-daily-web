type Query {
  posts: [Post!]
}

type Post {
  id: ID!
  title: String!
  subtitle: String
  content: String!
  category: Category
  published_at: String!
}

type Category {
  id: ID
  name: String!
  posts: [Post!]
}
